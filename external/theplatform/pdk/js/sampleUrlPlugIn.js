// the actual functions you want to call from your URL plugin


//alert("sampleUrlPlugIn is loaded!")
function tpToUpperCase(s)
{
	tpDebug("RUNNING tpToUpperCase")
	return s.toUpperCase();
}
tpDebug("tpToUpperCase is LOADED")
